import { FC, ReactElement } from "react";
import { TasksTemplate } from "../../features/tasks-template/tasks-template";

const Tasks: FC = (): ReactElement => {
  return <TasksTemplate />
}

export default Tasks