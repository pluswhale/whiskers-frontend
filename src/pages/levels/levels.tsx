import { FC, ReactElement } from "react"
import { LevelsTemplate } from "../../features/levels-template/levels-template"

const Levels: FC = (): ReactElement => {
  return <LevelsTemplate />
}

export default Levels